version: "3.8"
services:
  backend:
    image: ghcr.io/matienzo/bot-discord:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    deploy:
      replicas: 2
      restart_policy:
        condition: on-failure